"""Every user-facing string.

All messages are Russian, worded exactly as the legacy bot words them — including the
places where the wording is odd, such as the missing minus sign on a downward move
(PLAN.md §6.10a) and the near-identical ``/stop`` and ``/delete_my_data`` replies.

Centralised so the wording can be diffed against the old bot during dual run, and so
localisation later means adding a catalogue rather than rewriting handlers.
"""
