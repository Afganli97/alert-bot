"""Inline keyboards.

Empty by design: the legacy bot has no button UI and the first Python release reproduces
that. The module exists so adding buttons later is a new function here plus a router,
rather than a change to the update scope in several places at once.
"""
